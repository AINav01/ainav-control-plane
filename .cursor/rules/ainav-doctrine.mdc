---
description: AINav Control Plane doctrine — always apply
alwaysApply: true
---

# AINav doctrine

- **Company:** AINav, Inc. · **Edge:** AINav.Institute · **Product:** AINav Control Plane
- **Job C only:** dual human admit + DecisionRecords before privileged SoR writes
- **Not:** agent inventory (Job A), IdP replacement (Job B), Aether brand, fail-open demos
- **Pin:** Azure Static Web Apps (`mothership/www`); LIVE_PIN_OK = `/health.json` is JSON with `policy_digest`
- **Deploy:** `bash mothership/azure/deploy-swa.sh` after `az login`
- **DNS:** Cloudflare resolves domain; remove Squarespace; point at SWA — see `docs/DNS_PROPAGATION_TROUBLESHOOTING.md`
- **Commercial:** FIRST_OFFER L1 $28–40k → P-ADM $40–60k/yr; fixtures-first
- **Success:** LIVE_PIN_OK × proof day × signed L1 FIRST_OFFER
- **Multi-host admit:** Redis Lua playbook `docs/REDIS_LUA_DUAL_ADMIT.md` · script `scripts/redis/dual_consume.lua` · rule `.cursor/rules/redis-lua-dual-admit.mdc` — claim only after fixtures pass
