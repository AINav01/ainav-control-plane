---
description: Plane 2.4 MAJOR
alwaysApply: true
---

# Plane 2.4

- 2.3 rules still apply (tagged hash, propose, required ticket fields, gates, persist ledger).
- Dual seats: omit both → hold_pending_approval. One seat → incomplete. A==B → sod_denied. A≠B → dual_consumed_pending_effector.
- EffectLedger is SoR idempotency, not an effector.
- In-process ≠ U-DUAL Redis.
- V1: sha256:8d4a295b1dfb76f4169193012fdb7666fb199df66f45fe94c0bfe247648f4e10

cd agent-governance && PYTHONPATH=. python3 tests/test_plane_major.py
